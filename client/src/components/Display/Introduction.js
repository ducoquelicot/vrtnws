import React from 'react';

function Intro() {

    return(
        <p>
            Gebruik deze tool om een dataset op te zoeken of toe te voegen. Je kunt datasets zoeken via de titel of de tags. Controleer voor je een nieuwe dataset
            toevoegt eerst of de dataset misschien al bestaat.
        </p>
    );
}

export default Intro